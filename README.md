# dashboardCapstone

Dashboard EDA lowongan kerja menggunakan Streamlit.

## Cara menjalankan

```bash
pip install -r requirements.txt
streamlit run app.py
```

Dataset yang digunakan: `cleaned_all_job.csv`.
